# ByteBuddhi VS Code extension

Thin IDE client. See [docs/vscode.md](../docs/vscode.md) in the repository root.

```bash
cd vscode-extension
npm install
npm run compile
npm test
```

Press F5 in this folder with the Extension Development Host after compiling.
